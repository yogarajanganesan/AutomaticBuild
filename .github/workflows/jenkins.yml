name: Trigger Jenkins

on:
workflow_dispatch:

jobs:
trigger:
runs-on: ubuntu-latest

```
steps:
  - name: Trigger Jenkins Build
    run: |
      curl -X POST \
      --user "${{ secrets.JENKINS_USER }}:${{ secrets.JENKINS_TOKEN }}" \
      "${{ secrets.JENKINS_URL }}/job/AutomaticBuild/build"
```
